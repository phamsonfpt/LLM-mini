# === API & Web Server ===
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
httpx>=0.24.0

# === Core Data & Config ===
pydantic>=2.0.0
pydantic-settings>=2.0.0
numpy>=1.24.0

# === Vector DB & Search ===
qdrant-client>=1.7.0
rank-bm25>=0.2.2

# === Embedding Model ===
sentence-transformers>=2.2.2

# === LLM (Local) ===
# llama-cpp-python phải cài thủ công tùy GPU/CPU – xem README.md
# Ví dụ Apple Silicon: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/metal

# === LLM (Cloud - tuỳ chọn) ===
google-genai>=0.1.0
requests>=2.31.0

# === Multimodal Document Parsers ===
markitdown[all]>=0.1.0          # PDF, DOCX, PPTX, Excel, HTML → Markdown
PyMuPDF>=1.23.0                 # Trích xuất ảnh từ PDF theo layout reading order
Pillow>=10.2.0                  # Xử lý ảnh

# === Audio Transcription ===
# openai-whisper không tương thích Python 3.13 do llvmlite==0.36.0
# Dùng faster-whisper thay thế (hỗ trợ Python 3.8-3.13, nhanh hơn 4x)
faster-whisper>=1.0.0

# === Web Scraping & YouTube ===
beautifulsoup4>=4.12.0
youtube-transcript-api>=0.6.2

# === Caching & Utilities ===
cachetools>=5.3.3
psutil>=5.9.0

# === Deep Learning (GPU/MPS detection, Reranker) ===
torch>=2.0.0
